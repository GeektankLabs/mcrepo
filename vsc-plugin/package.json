{
  "name": "mcrepo-multi-context",
  "displayName": "mcrepo (Multi-Context)",
  "description": "Explorer mode badges for mcrepo multi-context repositories",
  "version": "0.0.5",
  "publisher": "local",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:mcrepoDecorations.refresh"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mcrepoDecorations.refresh",
        "title": "mcrepo (Multi-Context): Refresh Decorations"
      },
      {
        "command": "mcrepoDecorations.setWrite",
        "title": "🗂 ✏️ Set Write"
      },
      {
        "command": "mcrepoDecorations.setRead",
        "title": "🗂 👀 Set Read"
      },
      {
        "command": "mcrepoDecorations.setSleep",
        "title": "🗂 💤 Set Sleep"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "mcrepoDecorations.setWrite",
          "group": "navigation@mcrepo",
          "when": "explorerResourceIsFolder && resourceScheme == file && resourceFilename != '🧩 contracts' && resourceFilename != '🧾 docs' && resourceFilename != '🧪 tests' && resourceFilename != '🧠 skills'"
        },
        {
          "command": "mcrepoDecorations.setRead",
          "group": "navigation@mcrepo",
          "when": "explorerResourceIsFolder && resourceScheme == file && resourceFilename != '🧩 contracts' && resourceFilename != '🧾 docs' && resourceFilename != '🧪 tests' && resourceFilename != '🧠 skills'"
        },
        {
          "command": "mcrepoDecorations.setSleep",
          "group": "navigation@mcrepo",
          "when": "explorerResourceIsFolder && resourceScheme == file && resourceFilename != '🧩 contracts' && resourceFilename != '🧾 docs' && resourceFilename != '🧪 tests' && resourceFilename != '🧠 skills'"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.6.3",
    "@vscode/vsce": "^3.6.2"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
